# Compiled output
/dist
/node_modules
/build

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store
Thumbs.db

# Tests
/coverage
/.nyc_output
/test-results/
junit.xml

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# SSL certificates
/certs/*.pem
/certs/*.key
/certs/*.cert
/certs/*.crt
# Keep directory structure
!/certs/.gitkeep

# Temporary files
*.tmp
*.swp
*.swo
.cache/

# Configuration files that should not be in version control
*.secret
*.pem
*.key
*.cert

# MongoDB data directory if using local MongoDB
/data

# Redis dump file if using local Redis
dump.rdb

# Kafka logs if using local Kafka
/kafka-logs
/zookeeper-data

# Docker related
.dockerignore
docker-compose.override.yml

# Generated documentation
/documentation

# Dependency lock files (keep only one)
package-lock.json
# yarn.lock
# pnpm-lock.yaml 